apiVersion: v1
kind: Service
metadata:
  name: frontend 
  namespace: roboshop
spec:
  selector:
    project: roboshop
    component: frontend  
    tier: backend
  ports:
    - protocol: TCP
      port: {{ .Values.service.servicePort }}
      targetPort: 8080